---
// Import des utilitaires Astro et de la collection de contenu
import BaseLayout from '../layouts/Layout.astro';
import { getCollection } from 'astro:content';

// Récupérer uniquement les projets qui ont isFeatured: true dans leur Frontmatter
const featuredProjects = await getCollection('projects', ({ data }) => {
  return data.isFeatured === true;
});

// Tri par date de publication
featuredProjects.sort((a, b) => new Date(b.data.publishDate).getTime() - new Date(a.data.publishDate).getTime());
---

<BaseLayout title="[Votre Prénom Nom] | Portfolio Développeur Full-stack">
  <main class="max-w-6xl mx-auto px-4 py-12 md:py-24">
    
    <section class="mb-20 text-center">
      <h1 class="text-5xl md:text-7xl font-extrabold text-gray-800 mb-4">
        Salut, je suis [Votre Prénom]
      </h1>
      <p class="text-xl md:text-2xl text-gray-600 max-w-3xl mx-auto">
        Développeur **Full-stack** en recherche de **stage S6**. Je conçois des solutions web modernes, rapides et performantes avec l'écosystème JavaScript.
      </p>

      <div class="mt-8 space-x-4">
        <a 
          href="/about" 
          class="inline-block px-8 py-3 bg-primaire text-white font-semibold rounded-lg shadow-lg hover:bg-green-800 transition duration-300"
        >
          En savoir plus sur moi
        </a>
        <a 
          href="#contact" 
          class="inline-block px-8 py-3 text-primaire border-2 border-primaire font-semibold rounded-lg hover:bg-secondaire transition duration-300"
        >
          Me Contacter
        </a>
      </div>
    </section>

    <section id="projets" class="mb-20">
      <h2 class="text-3xl md:text-4xl font-bold text-gray-800 mb-10 border-b-4 border-primaire inline-block pb-1">
        Projets Phares ({featuredProjects.length})
      </h2>

      <div class="grid md:grid-cols-2 gap-10">
        {featuredProjects.map(project => (
          <article class="bg-white rounded-xl shadow-xl overflow-hidden hover:shadow-2xl transition duration-300 border border-secondaire">
            <a href={`/projects/${project.slug}`} aria-label={`Voir le projet ${project.data.title}`}>
              
              <div class="h-64 bg-gray-200 flex items-center justify-center">
                <img src={project.data.thumbnail} alt={`Vignette du projet ${project.data.title}`} class="w-full h-full object-cover"/>
              </div>

              <div class="p-6">
                <h3 class="text-2xl font-bold text-gray-800 mb-2 hover:text-primaire transition">
                  {project.data.title}
                </h3>
                <p class="text-gray-600 mb-4">{project.data.subtitle}</p>
                
                <div class="flex flex-wrap gap-2">
                  {project.data.technologies.map(tech => (
                    <span class="text-xs font-medium bg-primaire/10 text-primaire px-3 py-1 rounded-full">
                      {tech}
                    </span>
                  ))}
                </div>

                <p class="mt-4 text-sm text-primaire font-semibold flex items-center">
                    <svg class="w-4 h-4 mr-1" fill="currentColor" viewBox="0 0 20 20"><path d="M10 12a2 2 0 100-4 2 2 0 000 4z"></path><path fill-rule="evenodd" d="M.458 10C1.732 5.943 5.522 3 10 3s8.268 2.943 9.542 7c-1.274 4.057-5.064 7-9.542 7S1.732 14.057.458 10zM14 10a4 4 0 11-8 0 4 4 0 018 0z" clip-rule="evenodd"></path></svg>
                    Voir le Cas d'Étude
                </p>
              </div>
            </a>
          </article>
        ))}
      </div>
    </section>

    <section id="contact" class="mb-20">
      <h2 class="text-3xl md:text-4xl font-bold text-gray-800 mb-10 border-b-4 border-primaire inline-block pb-1">
        Contactons-nous
      </h2>
      <p class="text-lg text-gray-600 mb-8">
        Vous êtes intéressé(e) par mon profil Full-stack pour un stage S6 ? N'hésitez pas à me contacter.
      </p>

      <div class="grid md:grid-cols-2 gap-12">
        <div class="space-y-4">
            <p class="text-xl font-semibold text-gray-800">Email Académique :</p>
            <p class="text-lg text-gray-600">
                <a href="mailto:votre.email@universite.fr" class="text-primaire hover:underline">votre.email@universite.fr</a>
            </p>

            <p class="text-xl font-semibold text-gray-800 pt-4">Réseaux Professionnels :</p>
            <div class="flex space-x-6 text-2xl">
                <a href="[Votre Lien GitHub]" target="_blank" class="text-primaire hover:text-green-800 transition" aria-label="Mon Profil GitHub">
                    </a>
                <a href="[Votre Lien LinkedIn]" target="_blank" class="text-primaire hover:text-green-800 transition" aria-label="Mon Profil LinkedIn">
                    </a>
            </div>
            
            <p class="text-xl font-semibold text-gray-800 pt-4">Télécharger mon CV :</p>
            <a 
              href="/cv_prenom_nom.pdf" 
              download 
              class="text-lg text-primaire hover:underline flex items-center"
            >
                <svg class="w-5 h-5 mr-2" fill="none" stroke="currentColor" viewBox="0 0 24 24"><path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M4 16v1a3 3 0 003 3h10a3 3 0 003-3v-1m-4-4l-4 4m0 0l-4-4m4 4V4"></path></svg>
                cv_prenom_nom.pdf (PDF)
            </a>
        </div>
        
        </div>
    </section>

  </main>
</BaseLayout>